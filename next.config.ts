import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Three.js / postprocessing ship modern ESM; nothing special needed yet.
  reactStrictMode: true,
};

export default nextConfig;
